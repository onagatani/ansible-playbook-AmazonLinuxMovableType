---
# ImageMagick
- name: ImageMagick is installed
  yum: name={{ item }} state=present
  with_items:
   - ImageMagick-c++
   - ImageMagick-c++-devel
   - ImageMagick-devel
   - ImageMagick-perl
   - compat-ImageMagick
  tags: ImageMagick

